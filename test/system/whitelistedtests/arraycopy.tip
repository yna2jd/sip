array_copy(arr2) {
  var b, i;
  b = [ #arr2 of arr2[0] ];
  for (i : 0 .. #arr2) {
    b[i] = arr2[i];
  }
  return b;
}

main() {
  var arr, b, x;
  arr = [ 13, 7, -4, 14, 0 ];
  b = array_copy(arr);
  if (arr[1] != b[1]) error b[1];
  if (b[3] != 14) error b[3];
  return 0;
}
